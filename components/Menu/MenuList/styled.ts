import styled from "styled-components";

const List = styled.ul`
  display: grid;
  width: 80%;
  margin: 2rem auto;
  grid-template-columns: 1fr;
  grid-row-gap: 2.5rem;
  list-style: none;
`;

export default List;