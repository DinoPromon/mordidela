import React from "react";
import { PageContainer } from "@components/shared";
import { CouponsTitle } from "./styled";

const Coupons: React.FC = (props) => {
  return (
    <PageContainer>
      <CouponsTitle>Cupons</CouponsTitle>
    </PageContainer>
  );
};

export default Coupons;