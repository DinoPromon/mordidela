import React from "react";
import { PageContainer } from "@components/shared";
import { OrdersTitle } from "./styled";

const Coupons: React.FC = (props) => {
  return (
    <PageContainer>
      <OrdersTitle>Cupons</OrdersTitle>
    </PageContainer>
  );
};

export default Coupons;