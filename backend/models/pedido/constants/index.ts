export enum StatusPedido {
  REJEITADO = "rejeitado",
  PENDENTE = "pendente",
  CONFIRMADO = "confirmado",
}

export enum TipoEntrega {
  ENTREGA = "entrega",
  BALCAO = "balcao",
}

export enum TipoPagamento {
  DINHEIRO = "dinheiro",
  CREDITO = "credito",
  DEBITO = "debito",
}
